using UnityEngine;

public class HealthRecover : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        DadosGlobais.hpAtualJogador = -1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
